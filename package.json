{
  "name": "electricity-meter-tracker",
  "private": true,
  "type": "module",
  "engines": { "node": "^20.19.0 || >=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:domain": "tsc -p tsconfig.domain-test.json && node --test domain-tests/usage.test.mjs domain-tests/dailyUsage.test.mjs domain-tests/billingCycle.test.mjs domain-tests/forecast.test.mjs",
    "test:worker": "tsc -p tsconfig.worker-test.json && node --test worker-tests/api.test.mjs",
    "test:ui": "playwright test",
    "test": "npm run test:domain && npm run test:worker && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
